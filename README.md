# Id_Scanner
A lightweight FastAPI backend + single-file HTML/JS frontend that scans ID / insurance cards through a device camera (or an uploaded photo), runs OCR, extracts structured fields with dynamic heuristics, and cross-references the text against a payer database for an instant `payer_id` lookup.
